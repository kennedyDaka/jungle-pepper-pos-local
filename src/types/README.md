# Types

- `domain.ts` defines frontend domain contracts used by services and routes.
- `database.ts` is the placeholder for future generated Supabase database types.

Keep generated backend types separate from UI-facing domain types so the frontend does not become coupled to a single backend client.
